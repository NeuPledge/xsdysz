<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.debrief.dal.mysql.partymember.PartyMemberMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->
    <sql id="Base_Column_List2">
        id, debrief_student_id, name, student_number, branch_id, college_id, grade_id, satisfaction,
    base_satisfaction, dissatisfaction, modify_time
    </sql>

    <!-- 实时获取学校的党员其他评价内容 -->
    <select id="selectCollegeCommentByCollegeIdSharding" resultType="java.util.Map">
        select party_member_id, content from debrief_evaluate_result
        where `college_id` = #{collegeId,jdbcType=BIGINT};
    </select>

    <select id="selectUnequalGradeId" resultType="cn.iocoder.yudao.module.debrief.dal.dataobject.partymember.PartyMemberDO">
        SELECT
        <include refid="Base_Column_List2"/>
        FROM `debrief_party_member`
        where grade_id != #{gradeId,jdbcType=BIGINT}
        ORDER BY RAND() LIMIT #{size,jdbcType=BIGINT};
    </select>

</mapper>